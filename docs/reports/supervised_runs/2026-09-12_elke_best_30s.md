# Überwachter Run 2026-09-12 — Elke Best, 30 s (mp3_low)

> Plan: `docs/ANALYSE_VOKAL_MUSIK_NACH_RESTAURATION.md` · Modus: Restoration ·
> Bit-Tiefe: 24 · Interpreter: python3.10 · Dauer: ~22 min · `RUN_RC=0`
> Log/Output: `output/supervised_run/elke_30s_supervised_v1019.{log,wav}`

## 1. Ergebnis-Kernzahlen

| Kennzahl | Wert | Bewertung |
|---|---|---|
| Return-Code | 0 | ✅ vollständiger Lauf |
| VQI | 0.910 | world_class |
| singer_id | 0.850 | DSP-Ersatzpfad (Resemblyzer fehlte im Lauf-Env — behoben, siehe §4) |
| formant | 0.850 | ok |
| art | 0.999 | ok |
| OQS | 99.4 | ok |
| HPI | 0.812 | ok (Betriebsart restoration) |

## 2. Per-Phase-Witness / Stem-Stufen-Statistik

- **§SLR-1-Stem-Block wurde in diesem Lauf nicht aktiviert** (kein `§SLR-1*`-Log,
  kein KIM/KIM-Inst/Air-Presence-Eintrag): Material `mp3_low` + 30 s Dauer
  führte zu keiner Stem-Separation (nur `MergeStemsSOTA`-Initialisierung
  sichtbar). Die neue Air-Presence-Stufe (§SLR-1e2b) und der Präsenz/Formant-
  Pfad (phase_19) wurden daher **nicht** durch den Witness geprüft.
- **Folge-Schritt:** Der nächste überwachte Lauf muss ein Material mit
  Vokal-Separation verwenden (Kandidaten: `test_audio/vocals/opera_sibilance.wav`,
  `choir_breaths.wav` oder der volle Elke-Best-Song) — dann liefert der
  Witness-Report die Gate-Statistik `applied/rejected/reason` für KIM2,
  Air-Presence und KIM-Inst (§5 des Analyse-Plans).

## 3. Befunde (Übeltäter-Kandidaten)

1. **Resemblyzer-Ersatzpfad 5×** (`ML→DSP-Ersatzpfad aktiviert`,
   `RuntimeError: ResemblyzerPlugin not available`): Root-Cause = Lauf-Env
   (uv-python3.10) hatte `resemblyzer==0.1.4` und `setuptools` nicht
   installiert, obwohl in `requirements/requirements_aurik.txt` deklariert.
   **Behoben** (Todo t9): Pakete installiert, `ResemblyzerPlugin`-Import
   verifiziert — der nächste Lauf nutzt den SOTA-Pfad.
2. Keine weiteren Regressions-Befunde im Witness-Report (keine `finding`s
   außerhalb der erwarteten Fallbacks).

## 4. Fazit

Der Lauf war funktional sauber (VQI world_class, OQS 99.4). Die beiden neuen
Wohlklang-Pfade (Air-Presence, Präsenz/Formant) sind im Material nicht
angetriggert worden; ihre Witness-Statistik steht für den nächsten
Vokal-Separations-Lauf aus. Resemblyzer-Root-Cause ist behoben.
